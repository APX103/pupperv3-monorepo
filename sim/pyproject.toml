[project]
name = "pupperv3-sim"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "glfw>=2.10.0",
    "mujoco>=2.3.0",
    "numpy>=1.24",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
